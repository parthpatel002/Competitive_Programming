// BitSet is much more efficient than unordered_map, and should be used whenever possible.
// The size of bitset is fixed at compile time, i.e. it can’t be changed at runtime.

// Initialise a bitset with M bits
# define M 8
bitset<M> bset;

